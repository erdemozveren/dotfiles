source-file ~/.config/tmux/tmux.reset.conf
set-option -g default-terminal 'screen-256color'
set-option -g terminal-overrides ',xterm-256color:RGB'

# Prefix
unbind C-b
set -g prefix M-q
bind -n M-q send-prefix

# Navigation
bind -r -T prefix Tab next-window

set -g base-index 1              # start indexing windows at 1 instead of 0
set -g escape-time 0             # zero-out escape time delay
set -g history-limit 1000000     # increase history size (from 2,000)
set -g detach-on-destroy off     # don't exit from tmux when closing a session
set -g renumber-windows on       # renumber all windows when any window is closed
set -g set-clipboard on          # use system clipboard
set -g default-terminal "${TERM}"
setw -g mode-keys vi
set -g mouse on

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'egel/tmux-gruvbox'
# set desired theme options...
set -g @tmux-gruvbox 'dark256'
set -g @tmux-gruvbox-statusbar-alpha 'true'

# Focus/Unfocus pane with <F12> (same as prefix + z in tmux default)
bind-key -n F12 resize-pane -Z
# Re map resurrect restore
set -g @resurrect-restore 'R'
run '~/.tmux/plugins/tpm/tpm'

# to disable tmux-gruvbox right status must be after tpm
set -g status-right ''

